{
  "name": "requestscope",
  "version": "1.1.0",
  "private": true,
  "workspaces": [
    "api"
  ],
  "scripts": {
    "dev": "npm --workspace api run dev",
    "test": "npm --workspace api test",
    "typecheck": "npm --workspace api run typecheck",
    "deploy:api": "npm --workspace api run deploy",
    "deploy:web": "wrangler pages deploy web --project-name requestscope"
  }
}
